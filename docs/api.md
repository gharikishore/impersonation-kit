# API reference

**Status:** placeholder — fleshed out in #953 after core (#949), routes (#950), and UI (#951) implementations land.

## Surface

- `createImpersonation(config) → ImpersonationKit`
- `createImpersonateRoute(kit) → { POST, DELETE }`
- `createCandidatesRoute(kit) → { GET }`
- `<ImpersonationBanner kit={kit} theme={...} />`
- `<ImpersonatePage kit={kit} theme={...} />`

Full prose docs land in #953.
